name = "synthesizer"
description = "Thin Codex loader for the canonical master synthesizer prompt."
model = "gpt-5.6-sol"
model_reasoning_effort = "max"
developer_instructions = """
This file is only a compatibility loader. The canonical role is
`.claude/agents/synthesizer.md`; it is never copied into `.codex`.

Before doing any task work, read that canonical markdown file completely, including its YAML
frontmatter and final line. Follow it as the authoritative developer-level role instructions, then
follow the parent's invocation message. If the file is missing or cannot be read completely, stop and
report the failure. Preserve every output, citation, cap, schema, and no-git/commit contract exactly.
"""
