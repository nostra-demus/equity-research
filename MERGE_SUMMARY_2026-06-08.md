# Merge Summary — framework audit fixes (2026-06-08)

**Branch:** `claude/cranky-noyce-cb09f8` · **20 commits** ahead of `main` · nothing pushed to `main`.
**Net diff vs main:** `163 files, +2,773 / −19,234`.
> ⚠️ The large deletion count is **one chore commit** (`6cd571a`) untracking the generated cockpit snapshot `ui/web/public/data/` (104 files, ~19K lines of build output — now gitignored, regenerated by `npm run build`). The actual fix code is the **+2,773 insertions**. No source or research data was deleted.

**Provenance:** every change traces to the 106-agent adversarial audit ([FRAMEWORK_AUDIT_2026-06-08.md](FRAMEWORK_AUDIT_2026-06-08.md)); each fix is documented Issue/Solution/Why-better in [FRAMEWORK_FIXES_2026-06-08.md](FRAMEWORK_FIXES_2026-06-08.md). **Eval `PASS 3/3` after every commit.**

---

## What changed, by theme

### 1. The fatal-class trust gap — math is now executed & gated (HIGH)
- `6e7a03e` **F08/F09/F11/F12/F-EVAL-1** — deterministic scenario-math gate: synthesizer computes via Bash, persists `scenarios[]`, eval check M recomputes Σ(p×return) and FAILs on a wrong headline (caught the real TMCV +4.3% vs −4.4% defect); check N blocks scratch leaks; DCF/reverse-DCF execute their math; eval un-redded for binary pools.
- `e9985e2` **F09/F03** — executed-arithmetic rolled to the 4 remaining numeric agents; triage treats a manifest `fail` export as missing.
- `fcff91d` **F01/F17** — integrity finish-gate wired into `/research:full` *before* commit (deterministic validator stamps PROVISIONAL; verify-evidence + pre-mortem run in-path; eval check O).
- `213a5c1` + `2cf03b9` **F05/F13** — deterministic citation resolver (`resolve_citations.py`): the auditor must reconcile against a real grep, not its recollection; token-matched, unit-mismatch aware.

### 2. Extraction overhaul — content-sniffing + self-bootstrap (HIGH)
- `e3352a8` **F-EXTRACT/-DEP** — sniff content not extension (CIQ mislabels `.doc`=MHTML, `.rtf`=Word); MHTML/Word/HTML readers; auto-bootstrapping venv; loud-fail on missing deps. *Live-validated: 12/12 CRM files, 0 failures.*
- `213a5c1` **F02/F24/F04** — read-into-memory (no FUSE mmap deadlock); `reset_dimensions` (no silent truncation); unit/currency capture.
- `e4c2034` **F25/F35** — fixed-point floats (greppable); explicit "needs OCR" status for image-only PDFs.

### 3. Data-gap contract & discipline (HIGH/MEDIUM)
- `72179c8` **F19/F20/F06/F37/F38** — consensus & insider must come from the pool (no silent web/memory fill); robust abort-matcher; symmetric bull/bear steelman (master §9A + module-level).
- `e673e66` **F18/F41/F39** + F37 rollout — web-price 2-source corroboration; synthesizer "HARD GATES" hoist; eval check P (non-tautological edge).
- `2b3eece` **F40/F15** — factor-count fix; inverted-score flag.

### 4. Provenance, calibration & infra (MEDIUM)
- `c9c3912` **F07/F34** — RUN_METADATA derived from on-disk artifacts; cockpit caches only successful reads.
- `8d253d2` **F23** — triage parses the reporting period from the document, not Drive mtime.
- `e4c2034` **F30/F32/F27/F43/F14** — same-run cross-module guard; upstream-gap as a cap input; printed-page citations; business-quality band anchor; governance source-tier note.

### 5. Sector fluency (MEDIUM)
- `bf32e6d` **F-SECTOR-1** — `frameworks/SECTOR_OVERLAYS.md` (10 business types → required KPIs / red flags / valuation norm); business-identity §3a required-KPI checklist; revenue-drivers consumer; eval-protected.

### 6. Docs, fixture, chores
- `2ce79b1` audit report + fixes log · `49c6299`,`5f85005` F-SECTOR-1 logging · `2a3e1ab` doc restructure.
- `cc2ebef` recovered CRM specialist-layer fixture (rebuilt from transcripts, zero re-run).
- `a62afd9` `launch.json` backend · `6cd571a` gitignore the cockpit snapshot (the `npm run dev` footgun).

---

## Before merging — what to know
- **Eval is green** (`/research:eval all` → PASS 3/3) and was after every commit; the new gates (M/N/O/P, sector contract) are forward-looking, so the committed BG/HCG/TMCV fixtures still pass.
- **No engine `ui/` regressions:** server typecheck clean, control plane healthy.
- **Not yet validated on a live run:** the F08 finish-gate *end-to-end* (synthesizer emitting `scenarios[]` → gate recompute) — needs one full `/research:full` run (the CRM specialist layer is recovered; only the synthesizer step is unrun).
- **Intentional non-fixes:** F42 (eval cap-lint — false-positive risk), `.pptx` extraction (needs a dep), TMCV's historical artifact (left by owner choice).
