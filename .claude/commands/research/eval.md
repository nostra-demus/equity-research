---
description: Golden-run regression harness — deterministically assert the structural, schema, contract, and math invariants hold across committed research runs (the fixtures), so framework/agent/command changes can't silently regress the engine. Writes a dated eval report.
argument-hint: [RUN_OR_TICKER | all]
allowed-tools: Read, Glob, Bash, Write
---

You are the **regression harness**. The engine grows fast — modules, frameworks, and a widening command surface (verify-evidence, pre-mortem, expectations-gap, review-decisions, calibrate). A system without an eval is a demo: this is the deterministic test suite that proves a change did not break the contracts. You assert that the **committed runs** (the golden fixtures, e.g. BG and HCG) still satisfy the structural, schema, contract, and math invariants.

**You do NOT re-run agents or the pipeline.** LLM outputs are non-deterministic, so re-running and diffing is meaningless. Instead you check that the *already-committed artifacts* satisfy the contracts — a fast, repeatable, deterministic gate to run after any change to an agent, framework, command, or the synthesizer. You are READ-ONLY on all run artifacts; you write only a dated eval report. Grounded in `CLAUDE.md` (§18 decision set, §10 scenario math, §15 hygiene, §24 Avoid-Big-Risks rejector filters), `DECISION_LEDGER.md` (§5 record schema, §8 review schema), and `MODULE_PIPELINE.md` (no stray confirmation blocks). Arguments: `$ARGUMENTS`.

---

## 1. Resolve scope

`$ARGUMENTS`: empty/`all` → every run that has a `decision_record.json` (the fixtures). A path/ticker → just that run. Discover with `Glob analyses/*/decision_record.json`.

## 2. Run the invariant checks (deterministic)

Run `scripts/eval.py` via Bash. It applies, per run, the following invariants — each PASS / FAIL / N/A with detail. **Any FAIL fails the suite** (a regression). Absent *optional* artifacts (audit reports, reviews) are N/A, not FAIL.

- **A. Structural** — `final_thesis.md` exists and is non-trivial (> 1 KB); `RUN_METADATA.md` exists; `decision_record.json` exists; every module subfolder present has its `99_*-synthesis.md`.
- **B. Decision-record schema** (`DECISION_LEDGER` §5) — parses; all 38 canonical fields present; arrays are arrays, objects are objects; `schema_version` == "1.0". Additive fields (`scenarios`, `post_review_confidence_score`, `confidence_haircut`, `pre_mortem_verdict`, `edge_score`, `edge_proof`) are type-checked **when present** (never required — older records omit them).
- **B2. `decision_date` is a real date** — the `decision_date` *value* parses as an ISO date, not merely that the key exists. A garbage/null date otherwise sends every forward-looking gate (K–S) to a false "predates feature" N/A; this makes it a hard FAIL.
- **C. Decision enum + basket mapping** — `decision` ∈ the `CLAUDE.md` §18 allowed set; `basket` matches the §3 mapping for that decision; `paper_treatment` matches the mapping (keyword check).
- **D. Missing-price contract** — if `entry_price` is null, `paper_treatment` is a no-trade treatment and `notes` actually **addresses** the missing/indicative price (a specific phrase — "indicative", "unverified", "entry price is null", "no paper trade" — not merely any word containing the substring "price").
- **E. Numeric hygiene** — `expected_return_pct`, `downside_risk_pct`, `risk_reward`, `confidence_score`, `data_sufficiency_score` are number-or-null; scores within 0–100.
- **F. Review-schedule date math** — `review_schedule` has 30d/90d/180d/365d; each is a valid ISO date and equals `decision_date` + N days (recomputed). Catches the date-generation regression class.
- **G. Audit-report schema** (optional, if present) — `verification_report.json` / `pre_mortem.json` / `expectations_gap.json` parse and carry their required keys; a `verification_report` with verdict "Failed" fails the suite (a golden fixture must not be a failed run).
- **H. No stray confirmation blocks** (`MODULE_PIPELINE`) — no `<run>/<module>/*.md` file's last 20 lines contain a line matching `^Agent:\s+\S+\s*$`.
- **I. Decision ↔ thesis consistency** — the `decision_record.decision` string appears in `final_thesis.md` **on its "Decision:" line or in the "| Rating |" scorecard cell** (the JSON's headline matches the memo's headline) — not merely anywhere in the prose, which let "Buy" match "Buyback" or "Avoid" match the §24 "Avoid Big Risks" header while the real verdict differed.
- **J. Framework source contracts** (suite-level, run once) — two bodies of wiring are still present in the framework/agent files. **(i) §24 Avoid-Big-Risks:** `CLAUDE.md` §24 with all six filters; each module's `MODULE_RULES.md` carries its filter caps; the new red-flag IDs (`RF-CAP-004`, `RF-OWN-004`, `RF-MGT-004`) exist; `business-quality` has the 11th factor; the synthesizer carries the §24 gate step and rating cap. **(ii) §17 Catalyst module:** the `catalyst` module (`MODULE_RULES`, `01_catalyst-calendar`, `99_catalyst-synthesis`) exists with its §17 discipline and runs-last `depends_on`, and the synthesizer's §7 defers to it. **(iii) Three output tiers:** the `memo-writer` agent (`.claude/agents/memo-writer.md`) and the `/research:full` wiring (`.claude/commands/research/full.md`) that emit the colleague `memo.md` and the deterministic `audit_dossier.md` alongside `final_thesis.md` are present; AND the per-module tier wiring — the `module-memo-writer` agent (`.claude/agents/module-memo-writer.md`), the shared `frameworks/MODULE_PIPELINE.md` Step 4.9 (module memo + module dossier), and the `/research:rerun` refresh — that emit each module's `<module>_memo.md` and `<module>_dossier.md` alongside its `99_*-synthesis.md` are present. **(iv) Review-due scheduler:** the SessionStart hook (`.claude/settings.json` + `.claude/hooks/review_due.py`) that surfaces due decision reviews is present. **(v) Sector overlay consumers:** `frameworks/SECTOR_OVERLAYS.md` declares `03_margin-drivers` and `07_business-quality` as consumers of the sector KPI grammar; both agent files carry the `SECTOR_OVERLAYS.md` reference and the `sector overlay` step instruction (fix F-SECTOR-1). Without this wiring, a bank analyzed by the generic cost-stack agents produces margin analysis on raw-material costs and freight — the wrong metric grammar — and the business-quality factors for capital intensity, margin stability, and cyclicality are scored using a manufacturing lens on a balance-sheet-funded institution. This guard prevents silent drift where `SECTOR_OVERLAYS.md` is updated but its consuming agents are not. Guards the §24 implementation, the catalyst module, the three-output wiring, the scheduler, the evidence-integrity gate (`X_verify_floor`, `VERIFY_FLOOR_DATE`, `ACCEPTABLE_VERDICTS`), the §11 data-sufficiency cap (`Y_data_sufficiency_cap`, `INSUF_THRESHOLD`, `DATASUF_CONVICTION_FLOOR`, `HIGH_CONVICTION_DECISIONS`), and the sector-overlay consumer wiring (`SECTOR_OVERLAYS.md`, `sector overlay`, `No sector overlay` in both `03_margin-drivers.md` and `07_business-quality.md`) against silent deletion. Independent of any run; a missing anchor fails the suite.
- **K. §24 reflected in post-§24 runs** (forward-looking) — for a run whose `decision_date` is on/after the §24 landing date (`2026-06-03`), `final_thesis.md` must reference the Avoid-Big-Risks roll-up (the Headline Scorecard row or a §24 reference). Runs that predate §24 (the BG/HCG fixtures) are **N/A**, so the suite still passes; the check activates automatically for every new run.
- **L. Three output tiers present in post-landing runs** (forward-looking) — for a run whose `decision_date` is on/after the three-tier landing date (`2026-06-03`), both `memo.md` (the ~10-page colleague memo) and `audit_dossier.md` (the deterministic full-evidence concatenation) must exist beside `final_thesis.md`. Runs that predate the feature (the BG/HCG fixtures) are **N/A**, so the suite still passes; the check activates automatically for every new run.
- **M. Scenario-math reconciliation** (forward-looking; `fix F08/F12`) — for a run dated on/after `2026-06-08`, deterministically **recompute** the §10 identities from `decision_record.scenarios[]`: probabilities sum to 100; `expected_return_pct == Σ(probability × return_pct)` (1.0pp tolerance — catches sign flips); and, when a price anchor and per-case targets exist, the probability-weighted target and `risk_reward` reconcile too. This is the gate that the old check E (numeric *hygiene* only) never performed — the §10 math was previously enforced solely by LLM mental arithmetic. A post-gate run that quantified a return but omitted `scenarios[]` FAILs (the block is required so the math is checkable). Older fixtures lack `scenarios[]` → **N/A**.
- **N. No scratch-reasoning leak** (forward-looking; `fix F12`) — for a run dated on/after `2026-06-08`, `final_thesis.md` must not contain model self-correction text (e.g. "let me recalculate", "recomputing") — a committed thesis once shipped its raw arithmetic-correction scratch in §8. Older fixtures → **N/A**.
- **O. Integrity finish-gate present** (forward-looking; `fix F01`) — for a run dated on/after `2026-06-08` whose decision is in the Selected or Short basket, both `verification_report.json` and `pre_mortem.json` must exist — these are the in-path audits the `/research:full` finish-gate (step 10B) produces before commit; their absence means a conviction thesis shipped without the integrity + red-team pass. Older fixtures, and Watchlist/Avoid/Insufficient runs, → **N/A**.
- **P. Disconfirmation / edge quality** (forward-looking; `fix F39`) — for a run dated on/after `2026-06-08`, the variant perception must be **non-tautological** (`what_market_may_be_missing` ≠ `what_everyone_knows`; an explicitly-empty "no proven edge yet" is allowed per §7) and `kill_criteria` must carry at least one concrete falsification trigger. `kill_criteria` elements may be plain strings **or** objects (`{condition, what_it_means, …}` — the live synthesizer's format); both forms count. Catches a perfunctory bear case / a restated-consensus "edge" that the old field-presence check waved through. Older fixtures → **N/A**.
- **Q. Per-module three tiers present in post-landing runs** (forward-looking) — for a run whose `decision_date` is on/after the module-tiers landing date (`2026-06-08`), every module subfolder that has a `99_*-synthesis.md` must also carry a `*_memo.md` (the module memo) and a `*_dossier.md` (the module dossier) — the module-level equivalent of the run-level three tiers. Runs that predate the feature are **N/A**, so the suite still passes; the check activates automatically for every new run.
- **R. Memo-delta contract** (forward-looking; landing `2026-06-10`) — every review JSON filed on/after the landing date must carry the `DECISION_LEDGER` §8 `memo_delta` block, and any review that carries one must satisfy it: the paired `*_memo_delta*.md` exists on disk at `memo_delta_file` and stays a delta (≤ ~2,500 words — the 2–3-page discipline); `thesis_delta_verdict` is in-enum; every `changed_sections` entry cites an `evidence_source`; and any `rerun_recommended` names impacted module(s) that exist in the discovered agent roster. Pre-landing reviews and runs with no reviews are **N/A**.
- **S. Pre-mortem haircut propagated** (forward-looking; landing `2026-06-12`) — for a run whose `decision_date` is on/after the landing date, if `pre_mortem.json` exists and applied a haircut > 0, the decision_record must carry `post_review_confidence_score == pre_mortem.recommended_confidence` and `confidence_haircut`; `pre_mortem_verdict` must always be set (even at zero haircut). Older fixtures → **N/A**.
- **T. Forecast-ledger entry quality** (forward-looking; base gate `2026-06-13` / fix F-FL-1; probability-scale gate `2026-06-22`) — for a run dated on/after `2026-06-13` with a non-empty `forecast_ledger`, every entry must carry `prediction`, `confirmation_trigger`, `falsification_trigger`, and `time_window` (all non-empty strings), and `status` ∈ {open, confirmed, falsified, expired}. An entry missing a required field cannot be resolved in Phase 3 (`review-decisions`) and breaks Phase 4 calibration. Additionally, for runs dated on/after `2026-06-22`, each entry's `probability` must be a **number in [0, 100]** using the §10 percentage-point scale — `null` is allowed (entry is not Brier-scorable), but a non-numeric value, a value outside [0, 100], or a value in the open interval (0, 1) (a decimal-fraction form that would corrupt Brier-score computation) all FAIL. An empty `[]` is allowed per §19. Runs that predate the base gate, and runs with `forecast_ledger: []`, are **N/A**.
- **U. Post-mortem rating-cap consistency** (forward-looking; `fix F28b`; landing `2026-06-12`) — for a run dated on/after the landing date where `pre_mortem_verdict` is a terminal verdict ("Thesis broken" or "Does not survive — downgrade"), the finish-gate must have written `post_mortem_decision` and `post_mortem_basket`, and `post_mortem_basket` must NOT be "Selected" or "Short" — those verdicts mean the thesis does not hold as a conviction position. Closes the logical-contradiction gap where a "Strong Buy" decision coexists with a "Thesis broken" red-team verdict in the same committed record. The original `decision` / `basket` fields are preserved (the synthesizer's immutable call, required by check I); only the additive post-mortem fields are validated here. Older fixtures, and runs with non-terminal pre-mortem verdicts, → **N/A**.
- **V. Edge gate** (forward-looking; `CLAUDE.md` §7; landing `2026-06-15`) — the mechanical edge cap on confidence. For a run dated on/after the landing date: `edge_score` (when present) is a 0–100 number; a claimed real edge (`edge_score` ≥ 50) must carry a non-empty `edge_proof` (the falsifiable §7 item-4 test); and `confidence_score` > 60 requires a proven edge (`edge_score` ≥ 50 on a non-empty `edge_proof`) — restated consensus is not an edge. Older fixtures (pre-feature) → **N/A**.
- **W. Sector valuation-method consistency** (forward-looking; `SECTOR_OVERLAYS.md`; landing `2026-06-18`) — when `business_type` and `primary_valuation_method` are both set in the decision record, verify the method is not in the explicitly-forbidden list for that sector type (derived from the "NOT" declarations in `SECTOR_OVERLAYS.md`): Banks and lenders must not use FCFF; REITs and real-estate companies must not use EBITDA-DCF; Insurers must not use FCFF. Catches the categorical valuation error (e.g. a bank priced on FCFF DCF ignores that liabilities are the bank's raw material) before the thesis is treated as credible. When `business_type` or `primary_valuation_method` is absent (pre-gate runs or business-identity missing) → **N/A**.
- **X. Conviction-run evidence integrity** (forward-looking; `verify-evidence`; landing `2026-06-19`) — for a run in the Selected or Short basket dated on/after the landing date that carries a `verification_report.json`, the report's `verdict` must be `"Clean"` or `"Minor issues"`. A `"Material issues"` verdict means the verifier explicitly states the rating is provisional until `blocking_findings` are resolved — committing a conviction position while the evidence audit is unresolved is a false-confidence defect. Closes the evidence-audit trilogy: check G gates fabrication (`"Failed"`), check O gates existence (conviction runs must have a report), and check X gates the verdict quality (the report must clear the bar). Runs with no verification report, non-conviction runs (Watchlist, Avoid, Pair Trade, Insufficient Data), and pre-landing runs → **N/A**. The `X_verify_floor`, `VERIFY_FLOOR_DATE`, and `ACCEPTABLE_VERDICTS` symbols are protected by the J anchor in `scripts/eval.py`.
- **Y. Data-sufficiency ↔ decision cap** (`CLAUDE.md` §11; always-apply) — the mechanical enforcement of §11's two hard thresholds on `data_sufficiency_score`, as made concrete by the synthesizer's Rating Cap Rules ("<30 → must refuse to rate"; "30–49 → maximum rating Watchlist"). **(1)** Score < 30 ("insufficient — refuse to rate"): the decision **must** be `"Insufficient Data — Refuse To Rate"` (§18); any other decision claims conviction the data cannot support. **(2)** Score in [30, 50) ("weak — cap the opinion"): the maximum rating is `"Watchlist"`, so every rating **above** it is forbidden — that is every conviction position — the `"Selected"` long basket **and** `"Short Candidate"` (`HIGH_CONVICTION_DECISIONS` = `"Strong Buy"`, `"Buy"`, `"Starter Position Only"`, `"Short Candidate"`), matching how checks O/U/X define a conviction run (basket ∈ {`"Selected"`, `"Short"`}; check U caps a broken thesis to "Watchlist or lower" by forbidding both). A thin-data short is a capital-at-risk position with unbounded downside (§24). `"Watchlist"`, `"Avoid"`, `"Pair Trade / Hedge Required"` (a market-neutral hedge the §-distress rule already governs), and `"Insufficient Data"` sit at/below the Watchlist ceiling and are allowed at this band. **(3)** `data_sufficiency_score` absent or non-numeric **on a conviction rating** (the `"Selected"` longs or `"Short Candidate"`) → **FAIL**: the score is a required `/100` field (`DECISION_LEDGER.md` §5) and a null score must not buy a free pass a low score would be denied. Check E validates that `data_sufficiency_score` is a 0–100 number; this check validates what that number *implies for the rating*. No landing date — the rule is constitutional and all three committed fixtures (score 68–69, Watchlist/Avoid) pass trivially. `data_sufficiency_score` absent or non-numeric on any non-conviction rating → **N/A**. The `Y_data_sufficiency_cap`, `INSUF_THRESHOLD`, `DATASUF_CONVICTION_FLOOR`, and `HIGH_CONVICTION_DECISIONS` symbols are protected by the J anchor in `scripts/eval.py`.
- **Z. `thesis_type` enum + external-variable conviction cap** (`CLAUDE.md` §14; forward-looking; landing `2026-06-21`) — two gates in one. **(1) Enum membership:** every value in `thesis_type[]` must come from the `CLAUDE.md` §14 closed set, case-exact: `"Company-specific"`, `"Sector-cycle"`, `"Macro-conditional"`, `"Policy-conditional"`, `"Commodity-conditional"`, `"FX / rates"`, `"Liquidity / positioning"`, `"Governance turnaround"`, `"Balance-sheet survival"`, `"Pair trade / hedge"`, `"Insufficient data"`. Wrong casing (e.g. `"sector-cycle"` instead of `"Sector-cycle"`) silently breaks Phase 4 Brier-score calibration by thesis type, where consistent grouping is a prerequisite; the defect has been observed in committed runs. A **non-string** element (e.g. a stray object) is treated as an enum violation (**FAIL**), not a harness crash — membership is tested string-first so an unhashable element can never raise. An **empty** `thesis_type[]` is a **FAIL**: §14 requires every thesis to classify itself (use `"Insufficient data"` when it truly cannot rate), so a missing classification is a defect, not a free pass. **(2) Conviction cap:** when ANY `thesis_type` value is external-variable-dominant (`"Macro-conditional"`, `"Policy-conditional"`, `"Commodity-conditional"`, `"FX / rates"`, `"Liquidity / positioning"`) and the edge is not proven (`edge_score` < 50 or absent), the decision must be `≤ "Starter Position Only"` (the synthesizer's Rating Cap Rule: "Macro / commodity / policy-driven thesis with weak company-specific edge: maximum Starter Position Only"). `"Strong Buy"`, `"Buy"`, **and `"Short Candidate"`** sit above that ceiling — the short is included because eval.py already treats it as a conviction position (`HIGH_CONVICTION_DECISIONS`, checks O/U/X) and caps it on weak data like a Buy (check Y), so a no-edge external-variable short is the same false-confidence defect. When `edge_score` ≥ 50 (proven edge), the cap exception applies and any conviction rating is allowed. When `thesis_type` is **not a list** → **N/A** (schema check B already validates the type). Runs that predate the gate → **N/A**, so the golden suite stays green. The `eval_z_thesis_type_cap`, `THESIS_TYPE_ENUM`, `EXTERNAL_TYPES`, and `THESIS_Z_DATE` symbols are protected by the J anchor in `scripts/eval.py`.
- **AA. §18 module verdict-lock caps** (`CLAUDE.md` §18; forward-looking; landing `2026-06-23`) — the mechanical gate that enforces §18's two hard verdict-lock caps. The master synthesizer's prompt states these caps, but no deterministic check previously verified they were actually applied. This closes the same class of trust gap that checks M (scenario math), S (haircut propagation), and U (post-mortem rating cap) closed. For runs dated on/after the landing date, reads the committed module synthesis files for the balance-sheet-survival (BSS) module and the management-governance (MG) module, extracts each module's first `**Verdict:**` line via the regex `\*\*Verdict\*\*[^:\n]*:([^\n]+)`, and **FAILs** if a capping verdict coexists with a conviction decision that §18 forbids. **(1) BSS "Distress risk" cap:** when the BSS synthesis verdict contains `"Distress risk"`, the final decision must NOT be in `HIGH_CONVICTION_DECISIONS` (`"Strong Buy"`, `"Buy"`, `"Starter Position Only"`, `"Short Candidate"`) — §18 says "A balance-sheet 'Distress risk' verdict caps the headline at Watchlist or lower." Exception: when `thesis_type` includes `"Balance-sheet survival"`, the run is an explicit distressed/special-situation play and the cap does not apply. **(2) MG "Serious governance concerns" cap:** when the MG synthesis verdict contains `"Serious governance concerns"`, the final decision must NOT be a conviction position — §18 says "A governance hard disqualifier or critical flag caps the headline at Watchlist or lower." No exception applies to the governance cap: it fires regardless of thesis type. When a module folder is absent (the module did not run in this analysis), that cap check is individually **N/A** — only modules that actually ran can produce capping verdicts; it does not fail the suite that a module wasn't run. Runs where neither BSS nor MG ran → **N/A**. Runs that predate the gate → **N/A**, so the golden suite stays green. The `AA_module_verdict_lock`, `AA_DATE`, `BSS_CAP_VERDICT`, `MG_CAP_VERDICT`, and `eval_aa_module_verdict_lock` symbols are protected by the J anchor in `scripts/eval.py`.
- **AB. §13 BM disqualifier verdict-lock** (`CLAUDE.md` §13 / disqualifier-scan; forward-looking; landing `2026-06-24`) — the third pillar of the module verdict-lock trilogy, extending AA's BSS + MG coverage to the business-model module's hard disqualifier gate. The `disqualifier-scan` agent (`01_disqualifier-scan.md`) checks 8 hard facts (audit qualification or going concern, >50% promoter shares pledged, related-party transactions >25% of revenue, auditor changed twice without reason, material restatement >5%, active regulatory enforcement on financial reporting, >40% revenue from one customer without a long-term contract, negative operating cash flow in 3 of 4 years). Triggering **any one** locks the BM synthesis verdict to `"Low-quality business — avoid deeper work"`. For runs dated on/after the landing date, reads the committed `business-model/99_*-synthesis.md` and extracts its `**Verdict:**` line; if the verdict contains `"Low-quality business"`, the final decision must **NOT** be in `HIGH_CONVICTION_DECISIONS` — CLAUDE.md §13 says "a critical governance, solvency, accounting, fraud, or going-concern red flag must cap the final rating." **No exception applies**: unlike the BSS cap (which the `"Balance-sheet survival"` thesis type can bypass), the BM disqualifier cap fires regardless of thesis type — a disqualified company cannot receive conviction in any direction because the analysis base itself is unreliable. This matches the MG cap treatment (no exception). When the BM module folder is absent (the module did not run) → **N/A**. Runs that predate the gate → **N/A**, so the golden suite stays green. The `AB_bm_disqualifier_lock`, `AB_DATE`, `BM_CAP_VERDICT`, and `eval_ab_bm_verdict_lock` symbols are protected by the J anchor in `scripts/eval.py`.
- **AC. §24 Filter 2 turnaround conviction cap** (`CLAUDE.md` §24 Filter 2; forward-looking; landing `2026-06-27`) — mechanical enforcement of the synthesizer's Rating Cap Rule: "turnaround without ≥2–3 yrs delivered inflection → no better than 'Starter Position Only' on the turnaround alone." When a run's `thesis_type[]` includes `"Governance turnaround"` and the decision is `"Strong Buy"` or `"Buy"`, the run violates this cap. CLAUDE.md §24 Filter 2 states explicitly that the base rate of turnaround success is low and a turnaround thesis without proven operating improvement "carries a base-rate penalty and a conviction cap, and is classified honestly as a governance-turnaround thesis." That cap lived only in the synthesizer's prompt — this check closes the mechanical enforcement gap. **No edge-score bypass**: unlike the external-variable cap (check Z), the turnaround cap has no `edge_score` exception. If ≥2–3 yrs of delivered improvement were proven, the thesis would reclassify away from `"Governance turnaround"`; the type is therefore self-declaring that the inflection is unproven and the cap is unconditional. **`"Short Candidate"` is intentionally not capped** (`ABOVE_STARTER_AC = {"Strong Buy", "Buy"}`): shorting a failing turnaround is a distinct, valid high-conviction thesis with a different risk profile from owning it. When `thesis_type` is not a list → **N/A** (check B handles type). Runs where `"Governance turnaround"` is absent from `thesis_type[]` → **pass** (cap not triggered). Runs that predate the gate → **N/A**, so the golden suite stays green. The `AC_turnaround_cap`, `AC_DATE`, `TURNAROUND_TYPE`, `ABOVE_STARTER_AC`, and `eval_ac_turnaround_cap` symbols are protected by the J anchor in `scripts/eval.py`.
- **AD. §24 Filters 4 + 6 conviction cap** (`CLAUDE.md` §24 Filters 4/6; forward-looking; landing `2026-06-28`) — closes the final gap in the §24 mechanical enforcement family (AA covers BSS "Distress risk" + MG "Serious governance concerns"; AB covers BM "Low-quality business"; AC covers Filter 2 turnaround). **Filter 4 (serial acquirers):** `CLAUDE.md` §24 Filter 4 states that a serial-acquirer pattern is "close to a disqualifier" and must cap the capital-allocation score and conviction; `synthesizer.md` Rating Cap Rules make this explicit — `RF-CAP-004 → maximum "Watchlist"`. **Filter 6 (unaligned owners):** `CLAUDE.md` §24 Filter 6 states that a controller whose objective is not long-term per-share value creation is a structural cap — persistent cheapness under a misaligned owner is a value trap, not a margin of safety — `RF-OWN-004 → maximum "Watchlist"`. **Detection:** the standardised tags `RF-CAP-004` (serial acquirer) and `RF-OWN-004` (unaligned owner) are both surfaced in `management-governance/99_*-synthesis.md` — `RF-CAP-004` by the `02_capital-allocation-scorecard` agent and `RF-OWN-004` by the `04_ownership-and-insider-behavior` agent — when they identify a cap-level condition per `MODULE_RULES.md`. (The business-model module caps the Filter-4 score via its `11_capital-allocation-governance` agent but emits no `RF-CAP-004` tag string, so `RF-CAP-004` is read from the MG synthesis and scanned in the BM synthesis only defensively. Reading `RF-CAP-004` from the BM synthesis alone — as an earlier draft did — meant Filter 4 could never fire on a real run; see the `TMCV_2026-06-07` fixture, which fired `RF-CAP-004` in its MG synthesis only.) A conviction decision (`HIGH_CONVICTION_DECISIONS`) alongside a fired tag is a doctrine violation. **No bypass clause**: unlike the BSS cap (which the `"Balance-sheet survival"` thesis type can bypass for a distressed-play run), both Filter 4 and Filter 6 caps fire regardless of thesis type — a serial-acquirer or unaligned-owner company cannot receive a conviction position in any direction. `"Short Candidate"` is included in `HIGH_CONVICTION_DECISIONS` and is therefore also capped: a value-trap short against these companies belongs in `"Pair Trade / Hedge Required"`, not a standalone conviction short. `RF-CAP-004` is scanned in both the BM and MG synthesis; `RF-OWN-004` in the MG synthesis. When the MG module is absent → `RF-OWN-004` cannot be detected (Filter 6 **N/A**) and `RF-CAP-004` falls back to the BM synthesis; when both module syntheses are absent → full **N/A**. Runs that predate the gate → **N/A**, so the golden suite stays green. The `eval_ad_filter_4_6_cap`, `AD_DATE`, `CAP4_TAG`, `CAP6_TAG`, and `AD_filter_4_6_cap` symbols are protected by the J anchor in `scripts/eval.py`.
- **AE. §24 Filter 5 fast-changing-industry conviction cap** (`CLAUDE.md` §24 Filter 5; forward-looking; landing `2026-06-29`) — closes the last gap in the §24 mechanical enforcement family (AA/AB/AC/AD cover Filters 1-4 and 6). `CLAUDE.md` §24 Filter 5 states: "In industries that change fast, the winners are rarely knowable in advance … High rate-of-change / disruption risk lowers the business-quality score and caps conviction, and such a thesis is flagged as a sector / technology-cycle bet rather than a durable compounder." The synthesizer's Rating Cap Rules document this cap but, unlike AC (Filter 2) and AD (Filters 4+6), no eval check previously enforced it mechanically. **Detection:** `07_business-quality.md` emits the tag `RF-BQ-005 (fast-changing industry: rate-of-change ≤40)` as a **standalone line** when the industry rate-of-change / disruption row scores ≤40; `99_business-model-synthesis.md` propagates it. The check scans for a **fired standalone tag line** (a line whose leading token is `RF-BQ-005`, via `_tag_fired_standalone`) in **either the BM synthesis OR the `07_business-quality` source specialist** — not a bare substring. This (a) ignores a cleared/negation mention (`RF-BQ-005 not triggered`) or a cap-table-row mention where the tag is parenthetical, so a non-fired tag string does not false-positive; and (b) reads the source emitter as well as the roll-up, so a synthesis that forgets to propagate the tag cannot silently bypass the cap (`CLAUDE.md` §11: caps are applied, never silently overridden — mirrors how AD reads `RF-CAP-004` from its MG source). When `RF-BQ-005` fires and the decision is `"Strong Buy"` or `"Buy"` without a proven edge (`edge_score` ≥ 50), the run violates the cap — the ceiling is `"Starter Position Only"`. **Edge bypass:** `edge_score` ≥ 50 lifts the cap (a proven durable winner in a fast-moving industry earns the higher rating). **`"Short Candidate"` is intentionally not capped** (`ABOVE_STARTER_AE = {"Strong Buy", "Buy"}`): shorting a fast-changing-industry loser is a valid distinct thesis (cap logic for shorts lives in checks Y and Z). When the BM synthesis **and** the `07_business-quality` specialist are both absent → **N/A**. Runs that predate the gate → **N/A**, so the golden suite stays green. The `eval_ae_filter5_cap`, `AE_DATE`, `CAP5_TAG`, `ABOVE_STARTER_AE`, `AE_filter5_cap`, and `_tag_fired_standalone` symbols are protected by the J anchor in `scripts/eval.py`.

The script also notes (WARN, not FAIL) any **non-schema artifact** in a run folder (a stray file that is not a known schema / versioned-audit / review artifact) so coverage gaps and strays surface.

```bash
python3 scripts/eval.py "$ARGUMENTS"
```

## 3. Verdict, report, git

Read the script's output. Confirm the eval report parses (`python3 -m json.tool`). Print a human summary: overall **PASS/FAIL**, the per-run check matrix, any FAILs with detail, and any non-schema-file WARNs. Then commit straight to `main` (add only `analyses/eval/<DATE>_eval_report*.json`), message `Eval: <PASS|FAIL> — <n_runs> golden runs (<DATE>)`, and push. Report the SHA.

**If the suite FAILs:** report it plainly as a regression — name the run, the failed check, and the detail. Do not soften it; a FAIL means a contract broke and must be fixed before the engine is trusted.

---

## Hard rules

- **Deterministic only.** No agent/pipeline re-runs, no LLM judgment in the checks — pure structural/schema/contract/math assertions, so the result is reproducible and a real regression gate.
- **Read-only on run artifacts;** writes only the dated `analyses/eval/` report.
- **Any FAIL fails the suite.** Optional-artifact absence is N/A, not FAIL. A non-schema stray file is a WARN, not a FAIL.
- Grounded in `CLAUDE.md` §11/§18/§10/§15, `DECISION_LEDGER.md` §5/§8, `MODULE_PIPELINE.md`; spawns no subagents.
